import{r as e,b as n,a as t}from"./index-CGvuj-0X.js";import{g as r,h as a,p as i}from"./index-BeaafJ98.js";import"./react-C43tg6Pd.js";const s="\nstruct U {\n  res:    vec4<f32>,   // resolution.xy, texel.xy\n  grid:   vec4<f32>,   // gridCols, gridRows, atlasCols, atlasRows\n  adj0:   vec4<f32>,   // brightness, contrast, saturation, hue\n  adj1:   vec4<f32>,   // sharpness, gamma, colorMode, background\n  gradA:  vec4<f32>,   // rgb, time\n  gradB:  vec4<f32>,   // rgb, audio\n  misc:   vec4<f32>,   // effect, spacing, threshold, tilt\n  misc2:  vec4<f32>,   // spatialWeight, rampLen, atlasCount, _\n  p0:     vec4<f32>,   // effect params 0..3\n  p1:     vec4<f32>,   // effect params 4..7\n  m:      vec4<f32>,   // effect mode ints\n  post0:  vec4<f32>,   // bloom, bloomThreshold, scanlines, vignette\n  post1:  vec4<f32>,   // chromatic, grain, _, _\n};\n\n@group(0) @binding(0) var<uniform> u: U;\n@group(0) @binding(1) var<storage, read> ramp: array<u32>;\n@group(0) @binding(2) var samp: sampler;\n@group(0) @binding(3) var sampN: sampler;\n@group(0) @binding(4) var texSrc: texture_2d<f32>;\n@group(0) @binding(5) var texCells: texture_2d<f32>;\n@group(0) @binding(6) var texAtlas: texture_2d<f32>;\n@group(0) @binding(7) var texFx: texture_2d<f32>;\n\nstruct VSOut {\n  @builtin(position) pos: vec4<f32>,\n  @location(0) uv: vec2<f32>,\n};\n\n@vertex\nfn vs(@builtin(vertex_index) idx: u32) -> VSOut {\n  var pts = array<vec2<f32>, 3>(vec2<f32>(-1.0, -1.0), vec2<f32>(3.0, -1.0), vec2<f32>(-1.0, 3.0));\n  let p = pts[idx];\n  var out: VSOut;\n  out.pos = vec4<f32>(p, 0.0, 1.0);\n  // Flip Y so uv (0,0) is the top-left of the uploaded frame.\n  out.uv = vec2<f32>(p.x * 0.5 + 0.5, 0.5 - p.y * 0.5);\n  return out;\n}\n\nfn luma(c: vec3<f32>) -> f32 { return dot(c, vec3<f32>(0.2126, 0.7152, 0.0722)); }\n\nfn hash11(p: f32) -> f32 {\n  var x = fract(p * 0.1031);\n  x = x * (x + 33.33);\n  x = x * (x + x);\n  return fract(x);\n}\n\nfn hash12(p: vec2<f32>) -> f32 {\n  var p3 = fract(vec3<f32>(p.x, p.y, p.x) * 0.1031);\n  p3 = p3 + vec3<f32>(dot(p3, p3.yzx + 33.33));\n  return fract((p3.x + p3.y) * p3.z);\n}\n\nfn hash22(p: vec2<f32>) -> vec2<f32> {\n  var p3 = fract(vec3<f32>(p.x, p.y, p.x) * vec3<f32>(0.1031, 0.1030, 0.0973));\n  p3 = p3 + vec3<f32>(dot(p3, p3.yzx + 33.33));\n  return fract((p3.xx + p3.yz) * p3.zy);\n}\n\nfn vnoise(p: vec2<f32>) -> f32 {\n  let i = floor(p);\n  let f = fract(p);\n  let w = f * f * (3.0 - 2.0 * f);\n  let a = hash12(i);\n  let b = hash12(i + vec2<f32>(1.0, 0.0));\n  let c = hash12(i + vec2<f32>(0.0, 1.0));\n  let d = hash12(i + vec2<f32>(1.0, 1.0));\n  return mix(mix(a, b, w.x), mix(c, d, w.x), w.y);\n}\n\nfn rot2(a: f32) -> mat2x2<f32> {\n  let s = sin(a);\n  let c = cos(a);\n  return mat2x2<f32>(c, -s, s, c);\n}\n\nfn hueRotate(c: vec3<f32>, a: f32) -> vec3<f32> {\n  let k = vec3<f32>(0.57735);\n  let cs = cos(a);\n  return c * cs + cross(k, c) * sin(a) + k * dot(k, c) * (1.0 - cs);\n}\n\nfn rawAt(uv: vec2<f32>) -> vec3<f32> {\n  return textureSampleLevel(texSrc, samp, clamp(uv, vec2<f32>(0.0), vec2<f32>(1.0)), 0.0).rgb;\n}\n\n/** Source pixel with all global adjustments applied. */\nfn adjusted(uv: vec2<f32>) -> vec3<f32> {\n  var c = rawAt(uv);\n  let texel = u.res.zw;\n\n  if (u.adj1.x > 0.001) {\n    let blur = (rawAt(uv + vec2<f32>(texel.x, 0.0)) + rawAt(uv - vec2<f32>(texel.x, 0.0)) +\n                rawAt(uv + vec2<f32>(0.0, texel.y)) + rawAt(uv - vec2<f32>(0.0, texel.y))) * 0.25;\n    c = clamp(c + (c - blur) * u.adj1.x * 2.0, vec3<f32>(0.0), vec3<f32>(1.0));\n  }\n\n  c = clamp(c + u.adj0.x, vec3<f32>(0.0), vec3<f32>(1.0));\n  c = clamp((c - 0.5) * (1.0 + u.adj0.y * 1.6) + 0.5, vec3<f32>(0.0), vec3<f32>(1.0));\n\n  if (abs(u.adj0.w) > 0.0001) {\n    c = clamp(hueRotate(c, u.adj0.w), vec3<f32>(0.0), vec3<f32>(1.0));\n  }\n\n  let l0 = luma(c);\n  c = clamp(mix(vec3<f32>(l0), c, 1.0 + u.adj0.z), vec3<f32>(0.0), vec3<f32>(1.0));\n  c = pow(max(c, vec3<f32>(0.0)), vec3<f32>(1.0 / max(u.adj1.y, 0.001)));\n\n  let mode = i32(u.adj1.z + 0.5);\n  if (mode == 1) {\n    c = vec3<f32>(luma(c));\n  } else if (mode == 2) {\n    c = mix(u.gradA.rgb, u.gradB.rgb, clamp(luma(c), 0.0, 1.0));\n  }\n  return c;\n}\n\nfn adjL(uv: vec2<f32>) -> f32 { return luma(adjusted(uv)); }\n\n/* ------------------------------------------------------------- cells pass */\n\n@fragment\nfn fsCells(in: VSOut) -> @location(0) vec4<f32> {\n  let grid = u.grid.xy;\n  let cell = floor(in.uv * grid);\n  let cellSize = 1.0 / grid;\n  let origin = cell * cellSize;\n\n  var sum = vec3<f32>(0.0);\n  var lmin = 1.0;\n  var lmax = 0.0;\n  for (var y = 0; y < 4; y = y + 1) {\n    for (var x = 0; x < 4; x = x + 1) {\n      let o = (vec2<f32>(f32(x), f32(y)) + 0.5) * 0.25;\n      let s = adjusted(origin + o * cellSize);\n      sum = sum + s;\n      let sl = luma(s);\n      lmin = min(lmin, sl);\n      lmax = max(lmax, sl);\n    }\n  }\n  let avg = sum / 16.0;\n  var l = luma(avg);\n  let localContrast = lmax - lmin;\n  l = mix(l, clamp(l * 0.55 + localContrast * 1.35, 0.0, 1.0), u.misc2.x);\n  l = l + u.misc.w * ((in.uv.x + in.uv.y) * 0.5 - 0.5);\n  l = clamp((l - u.misc.z) / max(1.0 - u.misc.z, 0.001), 0.0, 1.0);\n  l = clamp(l * (1.0 + u.gradB.w), 0.0, 1.0);\n  return vec4<f32>(avg, l);\n}\n\n/* ------------------------------------------------------------- main pass */\n\nfn gradientAt(uv: vec2<f32>, op: i32) -> vec2<f32> {\n  let t = u.res.zw;\n  let tl = adjL(uv + t * vec2<f32>(-1.0, -1.0));\n  let tt = adjL(uv + t * vec2<f32>( 0.0, -1.0));\n  let tr = adjL(uv + t * vec2<f32>( 1.0, -1.0));\n  let ll = adjL(uv + t * vec2<f32>(-1.0,  0.0));\n  let cc = adjL(uv);\n  let rr = adjL(uv + t * vec2<f32>( 1.0,  0.0));\n  let bl = adjL(uv + t * vec2<f32>(-1.0,  1.0));\n  let bb = adjL(uv + t * vec2<f32>( 0.0,  1.0));\n  let br = adjL(uv + t * vec2<f32>( 1.0,  1.0));\n\n  if (op == 3) {\n    return vec2<f32>((tt + bb + ll + rr) - 4.0 * cc, 0.0);\n  }\n  var k = 2.0;\n  if (op == 1) { k = 1.0; }\n  if (op == 2) { k = 10.0; }\n  let gx = (tr + k * rr + br) - (tl + k * ll + bl);\n  let gy = (bl + k * bb + br) - (tl + k * tt + tr);\n  return vec2<f32>(gx, gy);\n}\n\n@fragment\nfn fsMain(in: VSOut) -> @location(0) vec4<f32> {\n  let uv = in.uv;\n  let effect = i32(u.misc.x + 0.5);\n  let base = adjusted(uv);\n  let bg = base * u.adj1.w;\n  let px = uv * u.res.xy;\n  var col = base;\n  var ink = 1.0;\n\n  if (effect == 0) {\n    /* ascii */\n    let grid = u.grid.xy;\n    let cell = floor(uv * grid);\n    let local = fract(uv * grid);\n    let info = textureSampleLevel(texCells, sampN, (cell + 0.5) / grid, 0.0);\n    let rampLen = i32(u.misc2.y);\n    let slot = clamp(i32(info.a * f32(rampLen - 1) + 0.5), 0, max(rampLen - 1, 0));\n    let glyph = f32(ramp[u32(slot)]);\n    let inset = u.misc.y * 0.5;\n    let g = (local - inset) / max(1.0 - u.misc.y, 0.001);\n    let inside = step(0.0, g.x) * step(g.x, 1.0) * step(0.0, g.y) * step(g.y, 1.0);\n    let ag = u.grid.zw;\n    let acell = vec2<f32>(glyph % ag.x, floor(glyph / ag.x));\n    let auv = (acell + clamp(g, vec2<f32>(0.0), vec2<f32>(1.0))) / ag;\n    ink = textureSampleLevel(texAtlas, samp, auv, 0.0).a * inside;\n    col = info.rgb;\n\n  } else if (effect == 1) {\n    /* waveLines */\n    let rows = max(u.p0.x, 1.0);\n    let row = floor(uv.y * rows);\n    let rowCenter = (row + 0.5) / rows;\n    let l = adjL(vec2<f32>(uv.x, rowCenter));\n    let disp = sin(uv.x * u.p0.z * 20.0 + u.p1.x + u.gradA.w * 1.5) * u.p0.y * (0.5 - l) / rows;\n    let d = abs(uv.y - (rowCenter + disp)) * rows;\n    ink = (1.0 - smoothstep(u.p0.w * 0.25, u.p0.w * 0.5, d)) * mix(0.35, 1.0, 1.0 - l);\n    col = adjusted(vec2<f32>(uv.x, rowCenter));\n\n  } else if (effect == 2) {\n    /* dithering (ordered) */\n    let levels = max(u.p0.x, 2.0);\n    let scale = max(u.p0.y, 1.0);\n    let dp = floor(px / scale);\n    let size = u.m.x;\n    var thr = 0.0;\n    let q2 = (dp % vec2<f32>(size)) / size;\n    // Interleaved-gradient ordering: a close ordered-dither stand-in that needs\n    // no matrix lookup and holds up at every matrix size.\n    thr = fract(52.9829189 * fract(0.06711056 * (dp.x) + 0.00583715 * (dp.y)));\n    thr = mix(thr, (q2.x + q2.y * 0.5), 0.35);\n    let s = adjusted((floor(px / scale) * scale + scale * 0.5) / u.res.xy);\n    col = clamp(floor(s * (levels - 1.0) + thr) / (levels - 1.0), vec3<f32>(0.0), vec3<f32>(1.0));\n\n  } else if (effect == 3) {\n    /* halftone */\n    let cell = max(u.p0.x, 1.0);\n    let ang = u.p0.y;\n    let rp = rot2(ang) * px;\n    let cid = floor(rp / cell);\n    let cc = (cid + 0.5) * cell;\n    let uvc = (rot2(-ang) * cc) / u.res.xy;\n    let l = adjL(uvc);\n    let radius = sqrt(clamp(1.0 - l, 0.0, 1.0)) * 0.72;\n    let d = (rp - cc) / cell;\n    let shape = i32(u.m.x + 0.5);\n    var dist = length(d);\n    if (shape == 1) { dist = max(abs(d.x), abs(d.y)); }\n    if (shape == 2) { dist = abs(d.x) + abs(d.y); }\n    if (shape == 3) { dist = abs(d.y); }\n    let edge = mix(0.22, 0.01, u.p0.z);\n    ink = 1.0 - smoothstep(radius - edge, radius + edge, dist);\n    col = adjusted(uvc);\n\n  } else if (effect == 4) {\n    /* dots */\n    let cell = max(u.p0.x, 1.0);\n    let cid = floor(px / cell);\n    let jit = (hash22(cid) - 0.5) * u.p0.w;\n    let cc = (cid + 0.5 + jit) * cell;\n    let uvc = cc / u.res.xy;\n    let l = adjL(uvc);\n    let radius = mix(u.p0.y, u.p0.z, 1.0 - l);\n    ink = 1.0 - smoothstep(radius - 0.03, radius + 0.03, length(px - cc) / cell);\n    col = adjusted(uvc);\n\n  } else if (effect == 5) {\n    /* contour */\n    let levels = max(u.p0.x, 2.0);\n    var l = adjL(uv);\n    if (u.p0.z > 0.001) {\n      var acc = 0.0;\n      for (var i = 0; i < 4; i = i + 1) {\n        let o = vec2<f32>(cos(f32(i) * 1.5708), sin(f32(i) * 1.5708)) * u.res.zw * (1.0 + u.p0.z * 3.0);\n        acc = acc + adjL(uv + o);\n      }\n      l = mix(l, acc * 0.25, u.p0.z);\n    }\n    let scaled = l * levels;\n    let band = abs(fract(scaled) - 0.5) * 2.0;\n    let w = fwidth(scaled) * u.p0.y * 2.0 + 0.001;\n    ink = 1.0 - smoothstep(1.0 - w * 2.0, 1.0, band);\n    if (i32(u.m.x + 0.5) == 1) {\n      col = vec3<f32>(floor(scaled) / levels);\n      ink = 1.0;\n    } else {\n      col = adjusted(uv);\n    }\n\n  } else if (effect == 6) {\n    /* edgeDetection */\n    let op = i32(u.m.x + 0.5);\n    let g = gradientAt(uv, op);\n    var mag = length(g);\n    if (op == 3) { mag = abs(g.x); }\n    mag = clamp(mag * u.p0.x, 0.0, 1.0);\n    mag = smoothstep(u.p0.y, min(u.p0.y + 0.35, 1.0), mag);\n    if (i32(u.m.y + 0.5) == 1) { mag = 1.0 - mag; }\n    ink = mag;\n    col = mix(adjusted(uv), vec3<f32>(1.0), 0.65);\n\n  } else if (effect == 7) {\n    /* crosshatch */\n    let spacing = max(u.p0.x, 1.0);\n    let layers = max(u.p0.z, 1.0);\n    let l = adjL(uv);\n    var acc = 0.0;\n    for (var i = 0; i < 6; i = i + 1) {\n      if (f32(i) >= layers) { break; }\n      let need = 1.0 - (f32(i) + 0.5) / layers;\n      if (l <= need) {\n        let a = u.p0.y + f32(i) * 0.9;\n        let v = (rot2(a) * px).y;\n        let line = abs(fract(v / spacing) - 0.5) * spacing;\n        acc = max(acc, 1.0 - smoothstep(u.p0.w * 0.5, u.p0.w, line));\n      }\n    }\n    ink = acc;\n    col = mix(adjusted(uv), vec3<f32>(0.85, 1.0, 0.85), 0.25);\n\n  } else if (effect == 8) {\n    /* blockify */\n    let block = max(u.p0.x, 1.0);\n    let cid = floor(px / block);\n    let cc = (cid + 0.5) * block;\n    col = adjusted(cc / u.res.xy);\n    let d = abs(px - cc) / (block * 0.5);\n    let n = mix(16.0, 2.0, u.p0.z);\n    let shape = pow(pow(abs(d.x), n) + pow(abs(d.y), n), 1.0 / n);\n    ink = 1.0 - smoothstep(1.0 - u.p0.y - 0.04, 1.0 - u.p0.y, shape);\n\n  } else if (effect == 9) {\n    /* threshold */\n    let l = adjL(uv);\n    var t = smoothstep(u.p0.x - u.p0.y - 0.001, u.p0.x + u.p0.y + 0.001, l);\n    if (i32(u.m.x + 0.5) == 1) { t = 1.0 - t; }\n    col = vec3<f32>(t);\n\n  } else if (effect == 10) {\n    /* noiseField */\n    var p = uv * u.p0.x;\n    var amp = 0.5;\n    var disp = vec2<f32>(0.0);\n    let oct = max(u.p0.w, 1.0);\n    for (var i = 0; i < 6; i = i + 1) {\n      if (f32(i) >= oct) { break; }\n      disp = disp + (vec2<f32>(vnoise(p + u.gradA.w * u.p0.y), vnoise(p.yx - u.gradA.w * u.p0.y * 0.8)) - 0.5) * amp;\n      p = p * 2.03;\n      amp = amp * 0.5;\n    }\n    col = adjusted(uv + disp * u.p0.z * 0.15 * (1.0 + u.gradB.w));\n\n  } else if (effect == 11) {\n    /* matrixRain */\n    let cols = max(u.p0.x, 1.0);\n    let rows = max(floor(cols * u.res.y / u.res.x), 1.0);\n    let cx = floor(uv.x * cols);\n    let cy = floor(uv.y * rows);\n    let seed = hash11(cx * 17.13);\n    let head = fract(seed + u.gradA.w * u.p0.y * (0.35 + seed * 0.65));\n    let rowNorm = (cy + 0.5) / rows;\n    let dist = fract(head - rowNorm);\n    let tail = 1.0 - smoothstep(0.0, max(u.p0.z, 0.02), dist);\n    let ag = u.grid.zw;\n    let glyph = floor(hash12(vec2<f32>(cx, cy + floor(u.gradA.w * 8.0))) * u.misc2.z);\n    let acell = vec2<f32>(glyph % ag.x, floor(glyph / ag.x));\n    let local = vec2<f32>(fract(uv.x * cols), fract(uv.y * rows));\n    let a = textureSampleLevel(texAtlas, samp, (acell + local) / ag, 0.0).a;\n    let mask = 1.0 - adjL(uv);\n    ink = a * tail * mix(0.25, 1.0, 1.0 - mask);\n    col = mix(adjusted(uv), vec3<f32>(0.6, 1.0, 0.6), 0.5) + smoothstep(0.06, 0.0, dist) * u.p0.w;\n\n  } else if (effect == 12) {\n    /* vhs */\n    let shift = u.p0.x * 0.01;\n    let line = floor(uv.y * u.res.y);\n    var wob = (hash11(line + floor(u.gradA.w * 24.0)) - 0.5) * u.p0.w * 0.02;\n    let bandSeed = floor(uv.y * 12.0 - u.gradA.w * 1.3);\n    let band = step(1.0 - u.p1.x * 0.35, hash11(bandSeed));\n    wob = wob + band * (hash11(bandSeed * 3.1) - 0.5) * 0.08 * u.p1.x;\n    let uv2 = vec2<f32>(uv.x + wob, uv.y);\n    col = vec3<f32>(adjusted(uv2 + vec2<f32>(shift, 0.0)).r, adjusted(uv2).g, adjusted(uv2 - vec2<f32>(shift, 0.0)).b);\n    col = col * (1.0 - u.p0.y * 0.5 * (0.5 + 0.5 * sin(uv.y * u.res.y * 3.14159)));\n    col = clamp(col + (hash12(px + u.gradA.w * 60.0) - 0.5) * u.p0.z * 0.6, vec3<f32>(0.0), vec3<f32>(1.0));\n\n  } else if (effect == 13) {\n    /* voronoi */\n    let cells = max(u.p0.x, 4.0);\n    let aspect = u.res.x / u.res.y;\n    let gridX = max(floor(sqrt(cells * aspect)), 1.0);\n    let gridY = max(floor(cells / gridX), 1.0);\n    let g = vec2<f32>(gridX, gridY);\n    let p = uv * g;\n    let cid = floor(p);\n    var best = 1e9;\n    var second = 1e9;\n    var bestCell = cid;\n    for (var y = -1; y <= 1; y = y + 1) {\n      for (var x = -1; x <= 1; x = x + 1) {\n        let nb = cid + vec2<f32>(f32(x), f32(y));\n        let site = nb + 0.5 + (hash22(nb) - 0.5) * u.p0.y;\n        let d = length(p - site);\n        if (d < best) { second = best; best = d; bestCell = nb; }\n        else if (d < second) { second = d; }\n      }\n    }\n    let siteUv = (bestCell + 0.5 + (hash22(bestCell) - 0.5) * u.p0.y) / g;\n    if (i32(u.m.x + 0.5) == 1) {\n      var acc = vec3<f32>(0.0);\n      for (var i = 0; i < 4; i = i + 1) {\n        let o = vec2<f32>(cos(f32(i) * 1.5708), sin(f32(i) * 1.5708)) / g * 0.25;\n        acc = acc + adjusted(siteUv + o);\n      }\n      col = acc * 0.25;\n    } else {\n      col = adjusted(siteUv);\n    }\n    ink = smoothstep(0.0, max(u.p0.z, 0.001), second - best);\n  }\n\n  return vec4<f32>(mix(bg, col, clamp(ink, 0.0, 1.0)), 1.0);\n}\n\n/* -------------------------------------------------------------- post pass */\n\n@fragment\nfn fsPost(in: VSOut) -> @location(0) vec4<f32> {\n  let uv = in.uv;\n  var col: vec3<f32>;\n\n  if (u.post1.x > 0.001) {\n    let dir = (uv - 0.5) * u.post1.x * 0.02;\n    col = vec3<f32>(\n      textureSampleLevel(texFx, samp, clamp(uv + dir, vec2<f32>(0.0), vec2<f32>(1.0)), 0.0).r,\n      textureSampleLevel(texFx, samp, uv, 0.0).g,\n      textureSampleLevel(texFx, samp, clamp(uv - dir, vec2<f32>(0.0), vec2<f32>(1.0)), 0.0).b);\n  } else {\n    col = textureSampleLevel(texFx, samp, uv, 0.0).rgb;\n  }\n\n  if (u.post0.x > 0.001) {\n    // 8-tap radial bright-pass in place of a separate blur chain.\n    var acc = vec3<f32>(0.0);\n    let r = u.res.zw * 6.0;\n    for (var i = 0; i < 8; i = i + 1) {\n      let a = f32(i) * 0.7854;\n      let s = textureSampleLevel(texFx, samp, clamp(uv + vec2<f32>(cos(a), sin(a)) * r, vec2<f32>(0.0), vec2<f32>(1.0)), 0.0).rgb;\n      acc = acc + s * smoothstep(u.post0.y, min(u.post0.y + 0.25, 1.0), luma(s));\n    }\n    col = col + acc * 0.125 * u.post0.x;\n  }\n\n  if (u.post0.z > 0.001) {\n    col = col * (1.0 - u.post0.z * 0.45 * (0.5 + 0.5 * sin(uv.y * u.res.y * 3.14159)));\n  }\n\n  if (u.post0.w > 0.001) {\n    let d = distance(uv, vec2<f32>(0.5)) * 1.414;\n    col = col * (1.0 - u.post0.w * smoothstep(0.35, 1.0, d));\n  }\n\n  if (u.post1.y > 0.001) {\n    col = col + (hash12(uv * u.res.xy + u.gradA.w * 97.0) - 0.5) * u.post1.y * 0.25;\n  }\n\n  return vec4<f32>(clamp(col, vec3<f32>(0.0), vec3<f32>(1.0)), 1.0);\n}\n",l=52,c=128,o=GPUShaderStage.FRAGMENT,u={ascii:0,waveLines:1,dithering:2,halftone:3,dots:4,contour:5,edgeDetection:6,crosshatch:7,blockify:8,threshold:9,noiseField:10,matrixRain:11,vhs:12,voronoi:13};function f(e){return{cells:e.createBindGroupLayout({entries:[{binding:0,visibility:o,buffer:{type:"uniform"}},{binding:2,visibility:o,sampler:{}},{binding:4,visibility:o,texture:{sampleType:"float"}}]}),main:e.createBindGroupLayout({entries:[{binding:0,visibility:o,buffer:{type:"uniform"}},{binding:1,visibility:o,buffer:{type:"read-only-storage"}},{binding:2,visibility:o,sampler:{}},{binding:3,visibility:o,sampler:{type:"non-filtering"}},{binding:4,visibility:o,texture:{sampleType:"float"}},{binding:5,visibility:o,texture:{sampleType:"float"}},{binding:6,visibility:o,texture:{sampleType:"float"}}]}),post:e.createBindGroupLayout({entries:[{binding:0,visibility:o,buffer:{type:"uniform"}},{binding:2,visibility:o,sampler:{}},{binding:7,visibility:o,texture:{sampleType:"float"}}]})}}function d(e,n,t,r,a){return e.createRenderPipeline({layout:e.createPipelineLayout({bindGroupLayouts:[t]}),vertex:{module:n,entryPoint:"vs"},fragment:{module:n,entryPoint:r,targets:[{format:a}]},primitive:{topology:"triangle-list"}})}function p(e,n,t,r){const a=e.beginRenderPass({colorAttachments:[{view:t,loadOp:"clear",storeOp:"store",clearValue:{r:0,g:0,b:0,a:1}}]});a.setPipeline(n),a.setBindGroup(0,r),a.draw(3),a.end()}let v=null,g=null,m=null;function x(){v=null,g=null,m=null}async function h(e){return v||(g||(g=async function(e){if(!navigator.gpu)return null;let n=null;try{const t=await navigator.gpu.requestAdapter({powerPreference:"high-performance"});if(!t)return null;if(n=await t.requestDevice(),!n)return null;m=t;const r=n.createShaderModule({code:s}),a=await(r.getCompilationInfo?.()),i=a?.messages?.filter(e=>"error"===e.type)??[];if(i.length)return e?.(new Error(`WGSL: ${i[0].message}`)),n.destroy?.(),null;const o=f(n);n.pushErrorScope("validation");const u={cells:d(n,r,o.cells,"fsCells","rgba8unorm"),main:d(n,r,o.main,"fsMain","rgba8unorm"),post:d(n,r,o.post,"fsPost","rgba8unorm")},v=32,g=GPUTextureUsage.TEXTURE_BINDING|GPUTextureUsage.COPY_DST|GPUTextureUsage.RENDER_ATTACHMENT,x=(e,t,r=0)=>n.createTexture({size:{width:e,height:t},format:"rgba8unorm",usage:g|r}),h=x(v,v),y=x(8,8),b=x(v,v),w=x(v,v,GPUTextureUsage.COPY_SRC),U=x(16,16);n.queue.writeTexture({texture:h},new Uint8Array(v*v*4).fill(255),{bytesPerRow:4*v},{width:v,height:v}),n.queue.writeTexture({texture:U},new Uint8Array(1024).fill(255),{bytesPerRow:64},{width:16,height:16});const P=n.createBuffer({size:208,usage:GPUBufferUsage.UNIFORM|GPUBufferUsage.COPY_DST}),j=n.createBuffer({size:512,usage:GPUBufferUsage.STORAGE|GPUBufferUsage.COPY_DST}),k=new Float32Array(l);k.set([v,v,1/v,1/v],0),k.set([8,8,1,1],4),k.set([0,0,0,0],8),k.set([0,1,0,0],12),k.set([0,0,0,0],16),k.set([0,0,0,0],20),k.set([0,0,0,0],24),k.set([0,1,1,0],28),n.queue.writeBuffer(P,0,k),n.queue.writeBuffer(j,0,new Uint32Array(c));const T=n.createSampler({magFilter:"linear",minFilter:"linear"}),A=n.createSampler({magFilter:"nearest",minFilter:"nearest"}),z=n.createBindGroup({layout:o.cells,entries:[{binding:0,resource:{buffer:P}},{binding:2,resource:T},{binding:4,resource:h.createView()}]}),S=n.createBindGroup({layout:o.main,entries:[{binding:0,resource:{buffer:P}},{binding:1,resource:{buffer:j}},{binding:2,resource:T},{binding:3,resource:A},{binding:4,resource:h.createView()},{binding:5,resource:y.createView()},{binding:6,resource:U.createView()}]}),G=n.createBindGroup({layout:o.post,entries:[{binding:0,resource:{buffer:P}},{binding:2,resource:T},{binding:7,resource:b.createView()}]}),L=256,B=n.createBuffer({size:L*v,usage:GPUBufferUsage.COPY_DST|GPUBufferUsage.MAP_READ}),C=n.createCommandEncoder();p(C,u.cells,y.createView(),z),p(C,u.main,b.createView(),S),p(C,u.post,w.createView(),G),C.copyTextureToBuffer({texture:w},{buffer:B,bytesPerRow:L},{width:v,height:v}),n.queue.submit([C.finish()]);const E=await n.popErrorScope();if(E)return e?.(new Error(`WebGPU validation: ${E.message}`)),n.destroy?.(),null;await B.mapAsync(GPUMapMode.READ);const R=new Uint8Array(B.getMappedRange());let M=0;for(let e=0;e<R.length;e+=4)M=Math.max(M,R[e],R[e+1],R[e+2]);B.unmap();for(const e of[h,y,b,w,U])e.destroy();return B.destroy(),P.destroy(),j.destroy(),M<8?(e?.(new Error("WebGPU probe rendered black; falling back.")),n.destroy?.(),null):{device:n,module:r,layouts:o}}catch(t){return e?.(t),n?.destroy?.(),null}}(e).then(e=>(v=e,g=null,e)),g))}async function y(s,{onFatal:o,onError:f,probed:v}={}){const g=v??await h(f);if(!g)return null;const{device:m,module:y,layouts:b}=g,w=s.getContext("webgpu");if(!w)return null;const U=navigator.gpu.getPreferredCanvasFormat();w.configure({device:m,format:U,alphaMode:"opaque"});let P=!1,j=!1,k=null,T=0,A=!1;const z=e=>{P||(P=!0,f?.(e instanceof Error?e:new Error(String(e))),o?.("webgl2"))};m.lost?.then(e=>{k={reason:e?.reason,message:e?.message},x(),j||z(`WebGPU device lost (${e?.reason??"unknown"}): ${e?.message??""}`)}),m.addEventListener?.("uncapturederror",e=>{z(`WebGPU error: ${e.error?.message??e.error}`)});const S={cells:d(m,y,b.cells,"fsCells","rgba8unorm"),main:d(m,y,b.main,"fsMain","rgba8unorm"),post:d(m,y,b.post,"fsPost",U)},G=m.createBuffer({size:208,usage:GPUBufferUsage.UNIFORM|GPUBufferUsage.COPY_DST}),L=m.createBuffer({size:512,usage:GPUBufferUsage.STORAGE|GPUBufferUsage.COPY_DST}),B=m.createSampler({magFilter:"linear",minFilter:"linear",addressModeU:"clamp-to-edge",addressModeV:"clamp-to-edge"}),C=m.createSampler({magFilter:"nearest",minFilter:"nearest"}),E=GPUTextureUsage.TEXTURE_BINDING|GPUTextureUsage.COPY_DST|GPUTextureUsage.RENDER_ATTACHMENT|GPUTextureUsage.COPY_SRC,R=(e,n)=>m.createTexture({size:{width:Math.max(1,e),height:Math.max(1,n)},format:"rgba8unorm",usage:E});let M=null,F=null,V=null,_=null,D={w:0,h:0},O={w:0,h:0},q={w:0,h:0},N="",Y={key:"",list:[]};const I=new Float32Array(l);return{name:"webgpu",canvas:s,device:m,resize(e,n){s.width===e&&s.height===n||(s.width=e,s.height=n),function(e,n){O.w===e&&O.h===n||(F?.destroy(),F=R(e,n),O={w:e,h:n})}(e,n)},render(s,l,o,f,d=0,v=0){if(P)throw new Error("webgpu backend is no longer usable");if(k)throw z(`WebGPU device lost (${k.reason}): ${k.message??""}`),new Error("webgpu device lost");this.resize(o,f);const g="ascii"===l.effect,x=g?e(l.ascii):["0","1","ｱ","ｲ","ｳ","ｴ","ｵ","ﾊ","ﾋ","ﾌ","ﾍ","ﾎ","A","Z"],h=n(x,{size:32});!function(e){e.key===N&&_||(_?.destroy(),_=R(e.canvas.width,e.canvas.height),m.queue.copyExternalImageToTexture({source:e.canvas},{texture:_},{width:e.canvas.width,height:e.canvas.height}),N=e.key)}(h),Y.key!==h.key&&(Y={key:h.key,list:t(h).reverse()});let y=Y.list;if(y.length>c){const e=y.length/c;y=Array.from({length:c},(n,t)=>y[Math.floor(t*e)])}const U=new Uint32Array(c);U.set(y.slice(0,c)),m.queue.writeBuffer(L,0,U);const j=g?r(l,o,f,h):{cols:1,rows:1};var E,O;E=j.cols,O=j.rows,q.w===E&&q.h===O||(V?.destroy(),V=R(E,O),q={w:E,h:O});const W=function(e){return{width:e.videoWidth??e.naturalWidth??e.width??0,height:e.videoHeight??e.naturalHeight??e.height??0}}(s);if(!W.width||!W.height)return;!function(e,n){D.w===e&&D.h===n||(M?.destroy(),M=R(e,n),D={w:e,h:n})}(W.width,W.height),m.queue.copyExternalImageToTexture({source:s,flipY:!1},{texture:M},{width:W.width,height:W.height}),function(e,n,t,r,s,l,c,o){const f=e.adjustments,d=I,p=a(f.gradientFrom),v=a(f.gradientTo),{floats:g,ints:x}=i(e.effect,e.effectParams[e.effect]??{},s);d.set([n,t,1/n,1/t],0),d.set([c.cols,c.rows,l.cols,l.rows],4),d.set([f.brightness,f.contrast,f.saturation,f.hue*Math.PI/180],8),d.set([f.sharpness,f.gamma,"mono"===f.colorMode?1:"gradient"===f.colorMode?2:0,f.backgroundIntensity],12),d.set([p[0],p[1],p[2],r],16),d.set([v[0],v[1],v[2],s],20),d.set([u[e.effect]??0,e.ascii.spacing,e.ascii.threshold,e.ascii.tilt],24),d.set([e.ascii.spatialWeight,o,l.count,0],28),d.set([g[0],g[1],g[2],g[3]],32),d.set([g[4],g[5],g[6],g[7]],36),d.set([x[0],x[1],x[2],x[3]],40),d.set([e.post.bloom,e.post.bloomThreshold,e.post.scanlines,e.post.vignette],44),d.set([e.post.chromatic,e.post.grain,0,0],48),m.queue.writeBuffer(G,0,d)}(l,o,f,d,v,h,j,y.length);const $=m.createBindGroup({layout:b.cells,entries:[{binding:0,resource:{buffer:G}},{binding:2,resource:B},{binding:4,resource:M.createView()}]}),X=m.createBindGroup({layout:b.main,entries:[{binding:0,resource:{buffer:G}},{binding:1,resource:{buffer:L}},{binding:2,resource:B},{binding:3,resource:C},{binding:4,resource:M.createView()},{binding:5,resource:V.createView()},{binding:6,resource:_.createView()}]}),H=m.createBindGroup({layout:b.post,entries:[{binding:0,resource:{buffer:G}},{binding:2,resource:B},{binding:7,resource:F.createView()}]});T++;const K=m.createCommandEncoder();g&&p(K,S.cells,V.createView(),$),p(K,S.main,F.createView(),X),p(K,S.post,w.getCurrentTexture().createView(),H),m.queue.submit([K.finish()]),A||(A=!0,this.verifyFirstFrame())},async verifyFirstFrame(){try{const e=async(e,n,t)=>{if(!e||n<1||t<1)return 0;const r=256*Math.ceil(4*n/256),a=m.createBuffer({size:r*t,usage:GPUBufferUsage.COPY_DST|GPUBufferUsage.MAP_READ}),i=m.createCommandEncoder();i.copyTextureToBuffer({texture:e},{buffer:a,bytesPerRow:r},{width:n,height:t}),m.queue.submit([i.finish()]),await a.mapAsync(GPUMapMode.READ);const s=new Uint8Array(a.getMappedRange());let l=0;for(let c=0;c<s.length;c+=4)l=Math.max(l,s[c],s[c+1],s[c+2]);return a.unmap(),a.destroy(),l},n=await e(M,D.w,D.h);if(P||n<8)return;const t=await e(F,O.w,O.h);!P&&t<1&&z("WebGPU rendered a black frame from a non-black source.")}catch(e){j||z(e)}},debugState:()=>({dead:P,lostInfo:k,frames:T,srcDims:{...D},cellDims:{...q},renderDims:{...O},atlasKey:N.slice(0,40),canvas:`${s.width}x${s.height}`}),isLost:()=>P,dispose(){P=!0,j=!0,M?.destroy(),F?.destroy(),V?.destroy(),_?.destroy(),G.destroy(),L.destroy();try{w.unconfigure?.()}catch{}}}}export{y as createWebGPUBackend,x as invalidateSharedDevice,f as makeLayouts,d as makePipeline,h as probeWebGPU,p as renderPass};
